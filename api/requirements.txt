# Runtime uslugi FastAPI (krok 2). Obraz bazowy: python:3.12-slim.
# Wersje minimalne (styl jak w requirements-dev.txt).
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
httpx>=0.27          # klient HTTP do Tiki (ekstrakcja w 2.3 + healthcheck w /health)
