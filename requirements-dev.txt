# Zaleznosci deweloperskie (testy).
# Runtime aplikacji FastAPI (krok 2) — potrzebny do testow jednostkowych config/modeli
# oraz do uruchomienia uslugi lokalnie bez Dockera.
-r api/requirements.txt

pytest>=8.0
httpx>=0.27          # klient HTTP do Tiki/FastAPI w testach integracyjnych
Pillow>=10.0         # generowanie skanu PNG do testu OCR
python-docx>=1.1     # generowanie DOCX do testu ekstrakcji natywnej
PyYAML>=6.0          # parsowanie docker-compose.yml w tescie spojnosci configu
